const fileCopier = require('./fileCopier');

// Input and output file paths
const inputFile = 'input.txt'; 
const outputFile = 'output.txt'; 

// Call the copyFile function from the module
fileCopier.copyFile(inputFile, outputFile);
